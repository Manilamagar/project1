# 1. Remove the file from the index (without deleting your local copy)
git rm --cached backend/app.js
git rm --cached Backend/app.js

# 2. Re-add the correct one (choose ONE folder name and stick to it, e.g., "Backend")
git add Backend/app.js
